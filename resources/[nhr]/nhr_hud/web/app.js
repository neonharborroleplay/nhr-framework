const status=document.querySelector('#status'),vehicle=document.querySelector('#vehicle');window.addEventListener('message',e=>{const d=e.data;if(d.action!=='update')return;document.body.style.display=d.visible?'block':'none';status.innerHTML=['health','armor','hunger','thirst','stress','voice'].map(k=>`<div class="pill ${k==='voice'?'voice':''} ${k==='voice'&&d.talking?'talking':''}" title="${k==='voice'&&d.radio>0?`radio ${d.radio}`:k}"><i style="width:${Math.max(0,Math.min(100,d[k]))}%"></i></div>`).join('');vehicle.style.display=d.vehicle?'block':'none';document.querySelector('#speed').textContent=d.speed;document.querySelector('#fuel').textContent=`${d.fuel}% FUEL`});
